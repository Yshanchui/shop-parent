spring:
  application:
    name: shop-gateway
  cloud:
    nacos:
      discovery:
        server-addr: nacos.orb.local:8848
      config:
        server-addr: nacos.orb.local:8848
        file-extension: yml
  profiles:
    active: dev